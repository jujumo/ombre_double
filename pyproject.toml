[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "sandbox"
version="1.0.0"
dependencies = [
    "numpy",
    "torch",
    "torchvision",
    "matplotlib",
]
